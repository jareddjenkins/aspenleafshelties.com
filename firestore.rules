rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Local emulator rule set: allow unrestricted reads and writes so refactor
    // work can focus on application behavior without production risk.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
