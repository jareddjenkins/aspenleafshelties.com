rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Temporary migration rule set: keep uploads working while we move off Azure
    // and consolidate images into the new bucket.
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
